{
  "name": "@synchromy/slop-score-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite --host 127.0.0.1",
    "typecheck": "tsc -b --pretty false"
  },
  "dependencies": {
    "@synchromy/slop-score-engine": "workspace:*",
    "@synchromy/slop-score-rules": "workspace:*"
  }
}
